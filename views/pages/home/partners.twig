<section id="partners" class="partners">
	<div class="partners__wrapper">
		<h2 class="partners__title">{{ post.get_field('partners_title') }}</h2>
		<div class="partners__grid partners__grid--spaced">
			{% for item in post.get_field('partner_logos') %}
				<div class="partners__item">
					{% include 'shared/media.twig' with {
						source: Image(item).src,
						alt: Image(item).alt,
						class: 'partners__item-image',
					}%}
				</div>
			{% endfor %}
		</div>
	</div>
</section>
